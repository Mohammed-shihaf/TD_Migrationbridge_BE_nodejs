tool: mocha
language: javascript
language_version: "22"
technique: "CI/CD Integration Test"
classification: "Test Execution"
target_files: [../../test/]
run:
  command: "npx mocha 'test/**/*.test.js'"
expects:
  tests_pass: true
verified_in_session: true
verified_result: "7/7 real tests passed across route-splitting, migration-status, and widgets routes (via supertest)"
