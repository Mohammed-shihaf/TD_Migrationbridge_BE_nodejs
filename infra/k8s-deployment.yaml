apiVersion: apps/v1
kind: Deployment
metadata:
  name: td-migrationbridge-be-nodejs
  labels:
    app: td-migrationbridge-be-nodejs
spec:
  replicas: 2
  selector:
    matchLabels:
      app: td-migrationbridge-be-nodejs
  template:
    metadata:
      labels:
        app: td-migrationbridge-be-nodejs
    spec:
      containers:
        - name: app
          image: td-migrationbridge-be-nodejs:18
          ports:
            - containerPort: 3000
          readinessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: td-migrationbridge-be-nodejs
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
  selector:
    app: td-migrationbridge-be-nodejs
